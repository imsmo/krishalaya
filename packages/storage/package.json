{
  "name": "@krishalaya/storage",
  "version": "0.1.0",
  "private": true,
  "description": "Object-storage primitives shared by every service that hands out a file: a self-contained AWS SigV4 query-string presigner (no AWS SDK). ONE implementation of URL signing for the whole monorepo — a second copy would mean two places to fix when a signing bug is found.",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest --passWithNoTests",
    "lint": "eslint \"src/**/*.ts\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5.4",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "@types/jest": "^29.5.13"
  }
}
