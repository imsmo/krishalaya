# infra/k8s/external-secrets/ai-services.yaml · sync AWS Secrets Manager -> k8s Secret krishalaya-ai-env (consumed by the ai-services chart)
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: ai-services-env
  namespace: krishalaya
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: krishalaya-ai-env
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: krishalaya-prod/ai/env   # a JSON secret in Secrets Manager holding every env var for this service
