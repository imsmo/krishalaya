# infra/k8s/external-secrets/admin-api.yaml · sync AWS Secrets Manager -> k8s Secret krishalaya-admin-api-env (consumed by the admin-api chart)
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: admin-api-env
  namespace: krishalaya
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: krishalaya-admin-api-env
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: krishalaya-prod/admin-api/env   # a JSON secret in Secrets Manager holding every env var for this service
